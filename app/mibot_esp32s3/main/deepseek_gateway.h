#pragma once

/* Keep the implementation in examples/deepseek_smoke so the production
 * controller only needs this small optional adapter header. */
#include "../../../examples/deepseek_smoke/esp32/deepseek_gateway.h"
